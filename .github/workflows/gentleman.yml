name: anything

on:
  push:

jobs:
  grab:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        domestic: ["loaf", "mutter", "building", "primitive", "isolate"]
        forth: ["bar", "movement", "subsequent", "dissatisfy"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Nd.jar
